# AGENTS.md

## Project

Simple snake game using Python's built-in `turtle` module. Single-file project, no external dependencies.

## Run

```
python snake.py
```

## Notes

- Python 3.14 environment. `pygame` does not install here (build errors). Use stdlib only.
- No tests, lint, or build tooling.
